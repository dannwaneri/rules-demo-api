# rules-demo-api

Minimal Cloudflare Worker with `POST /feedback`. Built to test whether AI code
review tools actually enforce the rules documented in `CLAUDE.md` and
`AGENTS.md`, rather than just reviewing the diff in isolation.
